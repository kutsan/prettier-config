# @kutsan/prettier-config

## Usage

Install the config package:

```sh
npm add --save-dev @kutsan/prettier-config
```

Then use it in your `prettier.config.ts` file:

```typescript
export { config as default } from '@kutsan/prettier-config'
```
